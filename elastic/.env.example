# Copy to elastic/.env and fill in. The file is gitignored.
# Generate values with:  openssl rand -base64 24
ELASTIC_PASSWORD=
KIBANA_PASSWORD=
# Must be at least 32 characters
KIBANA_ENCRYPTION_KEY=
